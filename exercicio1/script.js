const numeropar = Number(prompt("Coloque um número par."))
console.log(numeropar)

let res = numeropar % 2
console.log(res)

// // Sim o padrão é que o resto sempre fica em zero, quando coloco
// qualquer número par.

// Se escrevermos um número impar o resto fica em 1.

